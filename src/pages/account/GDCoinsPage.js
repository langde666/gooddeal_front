import AccountLayout from '../../components/layout/AccountLayout';

const GDCoinsPage = (props) => {
    return (
        <AccountLayout>
            <div className="account-gd-coins-page">
                Your GD Coins...
            </div>
        </AccountLayout>
    );
};

export default GDCoinsPage;
